{
  "name": "inturnx",
  "version": "1.0.0",
  "description": "InturnX - AI-Powered Internship Platform",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix client --legacy-peer-deps && npm install --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev": "concurrently \"npm run dev:client\" \"npm run dev:server\"",
    "build": "cd client && npm install --legacy-peer-deps && npm run build",
    "vercel-build": "cd client && npm install --legacy-peer-deps && npm run build",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
